mutation updateTodo($id: ID!, $text: String!, $userId: String!, $list: [String]) {
  updateTodo(id: $id, input:{text: $text, userId: $userId, list: $list}) {
    user {
      id
    }
    id
    text
    list
  }
}